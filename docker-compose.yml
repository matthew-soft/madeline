version: "3.9"

services:
  madeline:
    image: ghcr.io/madeline-bot/madeline-v2:master
    env_file: .env
    volumes:
      - ./logs:/app/logs
    networks:
      - madeline-network
    restart:
      on-failure

networks:
  madeline-network:
