version: "3.3"

services:
  madeline:
    image: ghcr.io/madeline-bot/madeline:master
    env_file: .env
    volumes:
      - ./logs:/app/logs
      - ./gcp-key.json:/app/gcp-key.json
    networks:
      - madeline-network
    restart:
      on-failure

networks:
  madeline-network:
